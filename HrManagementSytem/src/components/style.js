const Container = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    marginTop: "50px",
    marginBottom: "50px"
  };
  
  const Header = {
    fontSize: "36px",
    fontWeight: "bold",
    color: "#44014C",
    marginBottom: "30px"
  };
  
  const LabelContainer = {
    display: "flex",
    alignItems: "center",
    marginBottom: "10px"
  };
  
  const Label = {
    fontSize: "18px",
    color: "#44014C",
    marginRight: "10px"
  };
  
  const FormContainer = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  };
  
  const ErrorMessage = {
    fontSize: "14px",
    color: "red",
    marginTop: "10px"
  };
  
  export { Container, Header, LabelContainer, Label, FormContainer, ErrorMessage };